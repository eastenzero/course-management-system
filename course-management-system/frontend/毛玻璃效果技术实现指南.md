# 🛠️ 毛玻璃效果技术实现指南

## 📚 快速开始

### 1. 全局样式设置

首先创建全局毛玻璃样式文件：

**文件**: `src/styles/glass-theme.css`

```css
/* CSS变量定义 */
:root {
  /* 蓝色主题色彩 */
  --glass-primary: #1890ff;
  --glass-primary-light: #40a9ff;
  --glass-primary-dark: #096dd9;
  --glass-secondary: #69c0ff;
  --glass-accent: #91d5ff;
  
  /* 毛玻璃背景 */
  --glass-bg-primary: rgba(255, 255, 255, 0.15);
  --glass-bg-secondary: rgba(255, 255, 255, 0.08);
  --glass-bg-hover: rgba(255, 255, 255, 0.2);
  
  /* 边框和阴影 */
  --glass-border: rgba(255, 255, 255, 0.25);
  --glass-border-light: rgba(255, 255, 255, 0.18);
  --glass-shadow: 0 20px 60px rgba(24, 144, 255, 0.15);
  --glass-shadow-hover: 0 25px 70px rgba(24, 144, 255, 0.25);
  
  /* 模糊效果 */
  --glass-blur-strong: blur(14px) saturate(140%);
  --glass-blur-medium: blur(10px) saturate(120%);
  --glass-blur-light: blur(6px) saturate(110%);
}

/* 基础毛玻璃类 */
.glass-base {
  backdrop-filter: var(--glass-blur-medium);
  border: 1px solid var(--glass-border-light);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.glass-primary {
  background: var(--glass-bg-primary);
  backdrop-filter: var(--glass-blur-strong);
  border: 1px solid var(--glass-border);
  box-shadow: var(--glass-shadow);
  border-radius: 16px;
}

.glass-secondary {
  background: var(--glass-bg-secondary);
  backdrop-filter: var(--glass-blur-medium);
  border: 1px solid var(--glass-border-light);
  box-shadow: 0 10px 30px rgba(24, 144, 255, 0.08);
  border-radius: 12px;
}

.glass-hover:hover {
  transform: translateY(-4px);
  background: var(--glass-bg-hover);
  box-shadow: var(--glass-shadow-hover);
}
```

### 2. 页面背景设置

为学生和教师页面创建统一的背景：

```css
/* 页面背景 */
.glass-page-background {
  min-height: 100vh;
  background: 
    radial-gradient(1200px 600px at 10% 10%, rgba(24, 144, 255, 0.25), transparent 60%),
    radial-gradient(1000px 500px at 90% 20%, rgba(64, 169, 255, 0.25), transparent 60%),
    linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 50%, #bae6fd 100%);
  position: relative;
  overflow-x: hidden;
}

.glass-page-background::before {
  content: '';
  position: absolute;
  inset: 0;
  background: 
    radial-gradient(800px 400px at 20% 80%, rgba(24, 144, 255, 0.12), transparent 60%),
    radial-gradient(900px 450px at 80% 30%, rgba(105, 192, 255, 0.12), transparent 60%);
  filter: blur(20px) saturate(120%);
  z-index: -1;
}
```

## 🧩 核心组件实现

### 1. GlassCard 组件

**文件**: `src/components/glass/GlassCard.tsx`

```typescript
import React from 'react';
import { Card, CardProps } from 'antd';
import './GlassCard.css';

interface GlassCardProps extends Omit<CardProps, 'className'> {
  variant?: 'primary' | 'secondary' | 'accent';
  hoverable?: boolean;
  glassEffect?: 'strong' | 'medium' | 'light';
  className?: string;
}

export const GlassCard: React.FC<GlassCardProps> = ({
  variant = 'primary',
  hoverable = true,
  glassEffect = 'medium',
  className = '',
  children,
  ...props
}) => {
  const glassClasses = [
    'glass-card',
    `glass-card--${variant}`,
    `glass-effect--${glassEffect}`,
    hoverable ? 'glass-hover' : '',
    className
  ].filter(Boolean).join(' ');

  return (
    <Card
      className={glassClasses}
      bordered={false}
      {...props}
    >
      {children}
    </Card>
  );
};
```

**样式文件**: `src/components/glass/GlassCard.css`

```css
.glass-card {
  @apply glass-base;
  overflow: hidden;
  position: relative;
}

.glass-card--primary {
  @apply glass-primary;
}

.glass-card--secondary {
  @apply glass-secondary;
}

.glass-card--accent {
  background: rgba(145, 213, 255, 0.12);
  border-color: rgba(145, 213, 255, 0.3);
}

.glass-effect--strong {
  backdrop-filter: var(--glass-blur-strong);
}

.glass-effect--medium {
  backdrop-filter: var(--glass-blur-medium);
}

.glass-effect--light {
  backdrop-filter: var(--glass-blur-light);
}

/* 卡片内容样式 */
.glass-card .ant-card-body {
  padding: 24px;
  position: relative;
  z-index: 1;
}

.glass-card .ant-card-head {
  background: transparent;
  border-bottom: 1px solid rgba(255, 255, 255, 0.15);
}

.glass-card .ant-card-head-title {
  color: #1f2937;
  font-weight: 600;
}
```

### 2. GlassTable 组件

**文件**: `src/components/glass/GlassTable.tsx`

```typescript
import React from 'react';
import { Table, TableProps } from 'antd';
import './GlassTable.css';

interface GlassTableProps<T = any> extends TableProps<T> {
  glassBackground?: boolean;
}

export const GlassTable = <T extends object = any>({
  glassBackground = true,
  className = '',
  ...props
}: GlassTableProps<T>) => {
  const tableClasses = [
    'glass-table',
    glassBackground ? 'glass-table--background' : '',
    className
  ].filter(Boolean).join(' ');

  return (
    <div className="glass-table-container">
      <Table
        className={tableClasses}
        {...props}
      />
    </div>
  );
};
```

### 3. 动态背景组件复用

复用登录页面的动态背景组件：

```typescript
// 在学生/教师页面中使用
import { DynamicBackground } from '@components/common';

const StudentDashboard: React.FC = () => {
  return (
    <div className="glass-page-background">
      <DynamicBackground 
        density={0.08} 
        speed={0.8} 
        lineMaxDist={120} 
        triMaxDist={80}
        color="rgba(24, 144, 255, 0.6)"
      />
      
      <div className="glass-content">
        {/* 页面内容 */}
      </div>
    </div>
  );
};
```

## 📱 响应式实现

### 断点管理

```css
/* 响应式断点 */
@media (max-width: 1199px) {
  .glass-card {
    margin: 12px;
  }
  
  .glass-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
  }
}

@media (max-width: 767px) {
  .glass-card {
    margin: 8px;
    border-radius: 12px;
  }
  
  .glass-grid {
    grid-template-columns: 1fr;
    gap: 12px;
  }
  
  /* 移动端优化毛玻璃效果 */
  .glass-primary {
    backdrop-filter: blur(8px) saturate(120%);
  }
  
  .glass-secondary {
    backdrop-filter: blur(6px) saturate(110%);
  }
}
```

## 🎨 主题定制

### 深色模式支持

```css
[data-theme='dark'] {
  --glass-bg-primary: rgba(31, 41, 55, 0.8);
  --glass-bg-secondary: rgba(31, 41, 55, 0.6);
  --glass-border: rgba(255, 255, 255, 0.1);
  --glass-border-light: rgba(255, 255, 255, 0.05);
}

[data-theme='dark'] .glass-page-background {
  background: 
    radial-gradient(1200px 600px at 10% 10%, rgba(24, 144, 255, 0.15), transparent 60%),
    radial-gradient(1000px 500px at 90% 20%, rgba(64, 169, 255, 0.15), transparent 60%),
    linear-gradient(135deg, #0f172a 0%, #1e293b 50%, #334155 100%);
}
```

## ⚡ 性能优化

### CSS优化技巧

```css
/* 使用transform和opacity进行动画，避免重排重绘 */
.glass-hover {
  will-change: transform, opacity;
  transition: transform 0.3s ease, opacity 0.3s ease;
}

/* 合理使用GPU加速 */
.glass-card {
  transform: translateZ(0);
  backface-visibility: hidden;
}

/* 避免过度使用backdrop-filter */
@supports not (backdrop-filter: blur(10px)) {
  .glass-card {
    background: rgba(255, 255, 255, 0.9);
  }
}
```

### JavaScript优化

```typescript
// 使用React.memo优化组件渲染
export const GlassCard = React.memo<GlassCardProps>(({
  // props
}) => {
  // 组件实现
});

// 使用useMemo缓存计算结果
const glassClasses = useMemo(() => [
  'glass-card',
  `glass-card--${variant}`,
  hoverable ? 'glass-hover' : '',
  className
].filter(Boolean).join(' '), [variant, hoverable, className]);
```

## 🔧 开发工具配置

### Tailwind CSS 配置

如果使用 Tailwind CSS，可以添加自定义类：

```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      backdropBlur: {
        'glass-light': '6px',
        'glass-medium': '10px',
        'glass-strong': '14px',
      },
      backgroundColor: {
        'glass-primary': 'rgba(255, 255, 255, 0.15)',
        'glass-secondary': 'rgba(255, 255, 255, 0.08)',
      }
    }
  }
}
```

---

**注**: 这个技术实现指南提供了具体的代码示例和最佳实践，可以直接用于项目开发。
