# 学生和教师页面开发规划

## 📋 现状分析

### ✅ 已有基础
- **用户模型**: 已区分admin/teacher/student用户类型
- **选课模型**: 已有Enrollment模型支持学生选课
- **权限系统**: 已有基于角色的权限控制
- **通用用户管理**: 已有基础的用户CRUD功能

### ❌ 缺失功能
- **学生专用页面**: 没有学生选课、成绩查看等专用功能
- **教师专用页面**: 没有教师课程管理、学生管理等专用功能
- **差异化界面**: 不同角色看到相同的通用界面
- **业务流程**: 缺少选课、退课、成绩管理等业务流程

---

## 🎯 开发目标

### 学生端功能
1. **个人选课系统**: 浏览课程、在线选课、退课
2. **个人课程表**: 查看已选课程的时间安排
3. **成绩查询**: 查看各科成绩和学分
4. **学习进度**: 跟踪学习状态和完成情况

### 教师端功能
1. **课程管理**: 管理自己教授的课程
2. **学生管理**: 查看选课学生名单和信息
3. **成绩录入**: 录入和管理学生成绩
4. **教学安排**: 查看和管理教学时间表

---

## 🏗️ 技术架构规划

### 后端开发 (Django)

#### 1. 创建学生应用 (apps/students/)
```
backend/apps/students/
├── __init__.py
├── apps.py
├── models.py          # 学生扩展信息模型
├── serializers.py     # 学生数据序列化
├── views.py          # 学生专用API视图
├── urls.py           # 学生API路由
├── permissions.py    # 学生权限控制
└── services.py       # 学生业务逻辑
```

#### 2. 创建教师应用 (apps/teachers/)
```
backend/apps/teachers/
├── __init__.py
├── apps.py
├── models.py          # 教师扩展信息模型
├── serializers.py     # 教师数据序列化
├── views.py          # 教师专用API视图
├── urls.py           # 教师API路由
├── permissions.py    # 教师权限控制
└── services.py       # 教师业务逻辑
```

#### 3. 扩展课程应用 (apps/courses/)
- 添加成绩管理模型 (Grade)
- 扩展选课API功能
- 添加课程评价功能

### 前端开发 (React)

#### 1. 学生页面模块 (src/pages/students/)
```
frontend/src/pages/students/
├── StudentsPage.tsx           # 学生主页面路由
├── dashboard/
│   └── StudentDashboard.tsx   # 学生仪表板
├── courses/
│   ├── CourseSelection.tsx    # 选课页面
│   ├── MyCourses.tsx         # 我的课程
│   └── CourseSchedule.tsx    # 课程表
├── grades/
│   ├── GradesList.tsx        # 成绩列表
│   └── GradeDetail.tsx       # 成绩详情
└── profile/
    └── StudentProfile.tsx     # 学生个人信息
```

#### 2. 教师页面模块 (src/pages/teachers/)
```
frontend/src/pages/teachers/
├── TeachersPage.tsx           # 教师主页面路由
├── dashboard/
│   └── TeacherDashboard.tsx   # 教师仪表板
├── courses/
│   ├── MyCourses.tsx         # 我的课程
│   ├── CourseStudents.tsx    # 课程学生
│   └── CourseSchedule.tsx    # 教学安排
├── grades/
│   ├── GradeEntry.tsx        # 成绩录入
│   └── GradeManagement.tsx   # 成绩管理
└── profile/
    └── TeacherProfile.tsx     # 教师个人信息
```

#### 3. 共享组件 (src/components/education/)
```
frontend/src/components/education/
├── CourseCard.tsx            # 课程卡片
├── ScheduleGrid.tsx          # 课程表网格
├── GradeTable.tsx           # 成绩表格
├── StudentList.tsx          # 学生列表
└── EnrollmentStatus.tsx     # 选课状态
```

---

## 📝 详细功能规划

### 学生端功能详情

#### 1. 学生仪表板 (StudentDashboard.tsx)
**功能**:
- 显示本学期选课统计
- 显示最近成绩和GPA
- 显示今日课程安排
- 显示重要通知和截止日期

**API需求**:
- `GET /api/v1/students/dashboard/` - 获取学生仪表板数据
- `GET /api/v1/students/today-schedule/` - 获取今日课程

#### 2. 选课系统 (CourseSelection.tsx)
**功能**:
- 浏览可选课程列表
- 按院系、时间、教师筛选课程
- 查看课程详情和选课人数
- 在线选课和退课
- 选课冲突检测

**API需求**:
- `GET /api/v1/students/available-courses/` - 获取可选课程
- `POST /api/v1/students/enroll/` - 选课
- `DELETE /api/v1/students/drop/{course_id}/` - 退课
- `POST /api/v1/students/check-conflicts/` - 检查选课冲突

#### 3. 我的课程 (MyCourses.tsx)
**功能**:
- 查看已选课程列表
- 查看课程详细信息
- 查看课程资料和作业
- 课程评价功能

**API需求**:
- `GET /api/v1/students/my-courses/` - 获取我的课程
- `GET /api/v1/students/course/{id}/detail/` - 获取课程详情
- `POST /api/v1/students/course/{id}/evaluate/` - 课程评价

#### 4. 个人课程表 (CourseSchedule.tsx)
**功能**:
- 周视图课程表展示
- 月视图课程安排
- 课程时间冲突提醒
- 导出课程表功能

**API需求**:
- `GET /api/v1/students/schedule/` - 获取个人课程表
- `GET /api/v1/students/schedule/export/` - 导出课程表

#### 5. 成绩查询 (GradesList.tsx)
**功能**:
- 按学期查看成绩
- 成绩统计和GPA计算
- 成绩趋势分析
- 成绩单导出

**API需求**:
- `GET /api/v1/students/grades/` - 获取成绩列表
- `GET /api/v1/students/gpa/` - 获取GPA统计
- `GET /api/v1/students/grades/export/` - 导出成绩单

### 教师端功能详情

#### 1. 教师仪表板 (TeacherDashboard.tsx)
**功能**:
- 显示本学期教学统计
- 显示学生选课情况
- 显示今日教学安排
- 显示待处理任务

**API需求**:
- `GET /api/v1/teachers/dashboard/` - 获取教师仪表板数据
- `GET /api/v1/teachers/today-schedule/` - 获取今日教学安排

#### 2. 我的课程 (MyCourses.tsx)
**功能**:
- 查看教授的课程列表
- 查看课程选课情况
- 管理课程信息和资料
- 发布课程通知

**API需求**:
- `GET /api/v1/teachers/my-courses/` - 获取我的课程
- `PUT /api/v1/teachers/course/{id}/` - 更新课程信息
- `POST /api/v1/teachers/course/{id}/notice/` - 发布通知

#### 3. 学生管理 (CourseStudents.tsx)
**功能**:
- 查看选课学生名单
- 查看学生详细信息
- 学生出勤管理
- 学生学习情况跟踪

**API需求**:
- `GET /api/v1/teachers/course/{id}/students/` - 获取选课学生
- `POST /api/v1/teachers/attendance/` - 记录出勤
- `GET /api/v1/teachers/student/{id}/progress/` - 学生进度

#### 4. 成绩管理 (GradeEntry.tsx)
**功能**:
- 批量录入学生成绩
- 成绩修改和审核
- 成绩统计分析
- 成绩导入导出

**API需求**:
- `POST /api/v1/teachers/grades/batch/` - 批量录入成绩
- `PUT /api/v1/teachers/grade/{id}/` - 修改成绩
- `GET /api/v1/teachers/course/{id}/grade-stats/` - 成绩统计

#### 5. 教学安排 (CourseSchedule.tsx)
**功能**:
- 查看教学时间表
- 申请调课和换课
- 教室使用情况查看
- 教学计划管理

**API需求**:
- `GET /api/v1/teachers/schedule/` - 获取教学安排
- `POST /api/v1/teachers/schedule/change-request/` - 申请调课
- `GET /api/v1/teachers/classroom-usage/` - 教室使用情况

---

## 🔧 数据模型扩展

### 1. 学生扩展模型 (StudentProfile)
```python
class StudentProfile(models.Model):
    user = models.OneToOneField(User, on_delete=models.CASCADE)
    admission_year = models.IntegerField()  # 入学年份
    major = models.CharField(max_length=100)  # 专业
    class_name = models.CharField(max_length=50)  # 班级
    gpa = models.DecimalField(max_digits=3, decimal_places=2)  # GPA
    total_credits = models.IntegerField(default=0)  # 总学分
```

### 2. 教师扩展模型 (TeacherProfile)
```python
class TeacherProfile(models.Model):
    user = models.OneToOneField(User, on_delete=models.CASCADE)
    title = models.CharField(max_length=50)  # 职称
    research_area = models.TextField()  # 研究方向
    office_location = models.CharField(max_length=100)  # 办公室
    office_hours = models.TextField()  # 答疑时间
```

### 3. 成绩模型 (Grade)
```python
class Grade(models.Model):
    student = models.ForeignKey(User, on_delete=models.CASCADE)
    course = models.ForeignKey(Course, on_delete=models.CASCADE)
    score = models.DecimalField(max_digits=5, decimal_places=2)
    grade_type = models.CharField(max_length=20)  # 平时/期中/期末
    semester = models.CharField(max_length=20)
    academic_year = models.CharField(max_length=10)
```

---

## 🚀 开发优先级

### 第一阶段 (高优先级)
1. **学生选课系统** - 核心业务功能
2. **教师课程管理** - 教师基础功能
3. **个人课程表** - 用户体验关键功能

### 第二阶段 (中优先级)
1. **成绩管理系统** - 完整业务闭环
2. **学生/教师仪表板** - 提升用户体验
3. **权限细化** - 安全性保障

### 第三阶段 (低优先级)
1. **高级功能** - 课程评价、数据分析
2. **移动端适配** - 扩展使用场景
3. **通知系统** - 增强用户粘性

---

## 📋 开发提示词

### 后端开发提示词
```
请为课程管理系统开发学生和教师专用功能模块：

1. 创建apps/students/应用，包含：
   - StudentProfile模型扩展学生信息
   - 学生选课API (选课、退课、查看可选课程)
   - 学生成绩查询API
   - 学生个人课程表API
   - 学生仪表板数据API

2. 创建apps/teachers/应用，包含：
   - TeacherProfile模型扩展教师信息
   - 教师课程管理API (查看教授课程、学生名单)
   - 教师成绩录入API
   - 教师教学安排API
   - 教师仪表板数据API

3. 扩展apps/courses/应用：
   - 添加Grade模型用于成绩管理
   - 扩展Enrollment模型支持选课状态
   - 添加课程评价功能

技术要求：
- 使用Django REST Framework
- 实现基于角色的权限控制
- 提供完整的API文档
- 包含数据验证和错误处理
- 支持分页和筛选功能
```

### 前端开发提示词
```
请为课程管理系统开发学生和教师专用前端页面：

1. 创建src/pages/students/目录，包含：
   - StudentDashboard.tsx - 学生仪表板
   - CourseSelection.tsx - 选课页面
   - MyCourses.tsx - 我的课程
   - CourseSchedule.tsx - 个人课程表
   - GradesList.tsx - 成绩查询

2. 创建src/pages/teachers/目录，包含：
   - TeacherDashboard.tsx - 教师仪表板
   - MyCourses.tsx - 我的课程管理
   - CourseStudents.tsx - 学生管理
   - GradeEntry.tsx - 成绩录入
   - CourseSchedule.tsx - 教学安排

3. 创建src/components/education/共享组件：
   - CourseCard.tsx - 课程卡片组件
   - ScheduleGrid.tsx - 课程表网格组件
   - GradeTable.tsx - 成绩表格组件
   - StudentList.tsx - 学生列表组件

技术要求：
- 使用React 18 + TypeScript
- 基于Ant Design 5.x UI库
- 实现响应式设计
- 集成ECharts图表展示
- 支持数据导出功能
- 包含完整的错误处理
- 遵循现有代码规范
```

### 路由配置提示词
```
请更新前端路由配置，添加学生和教师专用路由：

1. 在src/router/routes.tsx中添加：
   - /students/* 路由，仅student角色可访问
   - /teachers/* 路由，仅teacher角色可访问
   - 更新菜单权限配置

2. 在src/components/layout/MainLayout.tsx中：
   - 根据用户角色显示不同的菜单项
   - 学生看到：仪表板、选课、我的课程、成绩查询
   - 教师看到：仪表板、我的课程、学生管理、成绩录入

3. 实现角色重定向逻辑：
   - 学生登录后重定向到/students/dashboard
   - 教师登录后重定向到/teachers/dashboard
   - 管理员保持现有逻辑

要求：
- 保持现有路由结构不变
- 确保权限控制正确
- 提供平滑的用户体验
```

---

**文档创建时间**: 2025年1月14日  
**预估开发时间**: 3-4周  
**开发优先级**: 高  
**技术难度**: 中等
